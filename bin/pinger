#!/usr/bin/env ruby

require 'thor'
require File.expand_path(File.join('..','lib','pinger'), File.dirname(__FILE__))

host = ARGV[0] || 'google.com'

Pinger.ping_loop(host) do
  `say 'LINK DOWN'`
end
